version: '3'

services: 
    kuka-ros:
        build: 
            context: .
            dockerfile: Dockerfile.base
            args: 
                - ROS_DISTRO=noetic
        image: prachandabhanu/env:rostest